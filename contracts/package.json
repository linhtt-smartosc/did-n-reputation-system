{
  "name": "contracts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "deploy-local": "npx hardhat run scripts/1_entry-point-deploy.ts --network local && npx hardhat run scripts/2_smart_account_deploy.ts --network local && npx hardhat run scripts/3_did_contract_deploy.ts --network local && npx hardhat run scripts/4_vc_contract_deploy.ts --network local && npx hardhat run scripts/5_reputation_deploy.ts --network local"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@account-abstraction/contracts": "^0.7.0",
    "contracts": "file:",
    "ethers": "^6.13.2",
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-network-helpers": "^1.0.12",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
